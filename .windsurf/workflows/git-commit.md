---
auto_execution_mode: 2
description: Create git commit
---
Analyze the changes I have just made in the current session. Based on our Change Implementation Guidelines, generate a concise and professional commit message in English.
Format Rules:

Use FEAT: [description] if I added new tests, features, or adjustments.

Use FIX: [description] if I corrected a bug or a failing test.

Use DOCS: [description] if I only updated the README or docstrings.
Provide the commit message and, if possible, the terminal command git commit -m "[message]" ready to be executed.